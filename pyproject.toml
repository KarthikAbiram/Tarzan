[project]
name = "tarzan"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fire>=0.7.1",
    "pandas>=2.3.3",
    "tm-data-types>=0.2.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.17.0",
]
